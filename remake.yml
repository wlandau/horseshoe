sources: R/datasets
packages: fbseq
targets:
  all:
    depends: datasets
  datasets:
    depends:
    - paschold_DE_1000
    - poisson_DE_1000
  paschold_DE_1000:
    command: paschold_DE(genes = 1000)
  poisson_DE_1000:
    command: poisson_DE(genes = 1000)

